# ================================
# Development Environment
# ================================

environment=dev

# Application URLs
base.url=https://dev.example.com
api.base.url=https://api.dev.example.com

# Test Credentials
test.username=devuser
test.password=devpass123

# Database (if needed)
db.url=jdbc:mysql://localhost:3306/testdb
db.username=root
db.password=root

# Feature Flags
feature.new.login=true
